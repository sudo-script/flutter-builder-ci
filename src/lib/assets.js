/**
 * Named constants for every bundled asset. Screens import from here instead of
 * hard-coding paths, so renaming a file is a one-line change and a typo is a
 * build error rather than a blank image on device.
 */
export const AppImages = {
  // Add images in the builder to generate constants here.
};

export const AppAudio = {
  // Add audio in the builder to generate constants here.
};

export const AppVideo = {
  // Add video in the builder to generate constants here.
};

export const AppFiles = {
  // Add files in the builder to generate constants here.
};

export const AppFonts = {
  // Add fonts in the builder to generate constants here.
};
